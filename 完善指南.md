# 情绪色谱小程序 · 完善指南

## 一、课设要求核对

### 基本要求（及格线）
| 要求 | 状态 | 说明 |
|------|------|------|
| 登录界面 | ✅ 完成 | pages/login |
| 注册界面 | ✅ 完成 | pages/register |
| 列表图文显示 | ✅ 完成 | gallery页面显示色板卡片 |
| 选择功能 | ✅ 完成 | home页面情绪选择 |
| 提交功能 | ⚠️ 待完善 | 打卡/收藏需对接服务器 |
| 服务器通讯(JSON) | ⚠️ 模拟状态 | api.js已封装，需真实后端 |
| 服务器端接收信息 | ❌ 未实现 | 需部署Node.js后端 |
| 列表存储在服务器 | ❌ 未实现 | 需数据库存储色板数据 |

### 加分项（需选3条以上）
| 加分项 | 状态 | 已实现功能 |
|--------|------|------------|
| 手机文件操作(缓存) | ✅ | wx.setStorageSync存储用户信息、token |
| GPS定位 | ❌ 未实现 | 可添加：根据天气推荐配色 |
| 蓝牙/重力感应 | ✅ | 摇一摇随机配色(onAccelerometerChange) |
| 自定义组件 | ✅ | emotion-tag, palette-card, color-card |
| 网络API | ✅ | The Color API获取颜色名称 |
| 后端业务处理 | ⚠️ 部分 | api.js已封装算法，需对接真实服务 |
| 媒体类API | ⚠️ 部分 | 拍照取色已调用chooseMedia |
| 画布API | ✅ | Canvas画板绘图 |

**已满足加分项：5条** ✅

---

## 二、待完善清单

### 1. 后端服务部署（重要）

当前状态：api.js使用模拟数据，BASE_URL为占位符

**需要做：**
```javascript
// utils/api.js 修改
const BASE_URL = 'http://你的服务器地址/api'
```

**后端实现方案（选一）：**

方案A：云开发（推荐，无需自己部署服务器）
```javascript
// 使用微信云开发
wx.cloud.init()
wx.cloud.database() // 存储色板、用户数据
wx.cloud.callFunction() // 云函数处理业务
```

方案B：独立Node.js后端
```
server/
├── app.js          # Express服务
├── routes/
│   ├── user.js     # 用户注册/登录
│   ├── palette.js  # 色板CRUD
│   └── mood.js     # 打卡记录
└── models/
│   └── User.js     # 用户模型
│   └── Palette.js  # 色板模型
```

### 2. 数据持久化

**当前问题：**
- 色板数据是硬编码的mock数据
- 打卡记录不保存
- 收藏不保存

**需要实现：**
- 用户注册/登录 → 服务器验证
- 获取色板列表 → 从数据库读取
- 打卡记录 → 写入数据库
- 收藏操作 → 更新用户favorites字段

### 3. Bug修复清单

#### 页面功能Bug

| 问题 | 文件 | 修复方案 |
|------|------|----------|
| home.js检测登录后跳转login死循环 | pages/home/home.js:14 | 移除强制跳转，改为提示 |
| mine.js同样有登录检测 | pages/mine/mine.js:26 | 同上处理 |
| canvas画板roundRect兼容性 | pages/canvas/canvas.js:47 | 添加polyfill或降级处理 |
| tools.js摇一摇可能多次触发 | pages/tools/tools.js:52 | 添加防抖逻辑 |
| gallery无分页加载 | pages/gallery/gallery.js:36 | 实现真正的分页 |

#### 代码修复示例

```javascript
// pages/home/home.js 修复登录检测
checkLogin() {
  const userInfo = wx.getStorageSync('userInfo')
  if (userInfo) {
    this.setData({ userInfo })
  } else {
    // 不强制跳转，显示提示
    this.setData({ 
      userInfo: { username: '游客', avatar: '🎨' },
      isGuest: true 
    })
  }
}
```

```javascript
// pages/canvas/canvas.js roundRect兼容
// Canvas 2D API兼容处理
if (!ctx.roundRect) {
  ctx.roundRect = function(x, y, w, h, r) {
    if (w < 2 * r) r = w / 2
    if (h < 2 * r) r = h / 2
    this.beginPath()
    this.moveTo(x + r, y)
    this.arcTo(x + w, y, x + w, y + h, r)
    this.arcTo(x + w, y + h, x, y + h, r)
    this.arcTo(x, y + h, x, y, r)
    this.arcTo(x, y, x + w, y, r)
    this.closePath()
    return this
  }
}
```

### 4. 功能完善

#### 4.1 打卡功能完整实现
```javascript
// pages/home/home.js
onCheckin() {
  // 1. 选择情绪
  // 2. 生成配色
  // 3. 保存到数据库
  // 4. 更新mine页日历
}
```

#### 4.2 收藏功能
```javascript
// 当前只显示toast，需要：
// 1. 调用api.favoritePalette()
// 2. 更新mine页favorites列表
// 3. 同步本地缓存
```

#### 4.3 分享功能
```javascript
// 添加到每个palette-card
onShare() {
  wx.showShareMenu({
    withShareTicket: true,
    menus: ['shareAppMessage', 'shareTimeline']
  })
}
```

### 5. 可选加分项实现

#### 5.1 GPS定位 + 天气API（加分项②）
```javascript
// pages/home/home.js 添加
onGetLocation() {
  wx.getLocation({
    type: 'wgs84',
    success: (res) => {
      // 调用天气API
      wx.request({
        url: `https://api.weather.com/...?lat=${res.latitude}&lon=${res.longitude}`,
        success: (weather) => {
          // 根据天气推荐配色
          this.recommendByWeather(weather.data)
        }
      })
    }
  })
}
```

#### 5.2 媒体API完善（加分项⑦）
```javascript
// pages/tools/tools.js 拍照取色
onTakePhoto() {
  wx.chooseMedia({
    success: (res) => {
      // 真正提取颜色 - 使用canvas读取像素
      this.extractColors(res.tempFiles[0].tempFilePath)
    }
  })
}

extractColors(filePath) {
  const ctx = wx.createCanvasContext('extractCanvas')
  ctx.drawImage(filePath, 0, 0, 100, 100)
  ctx.draw(false, () => {
    wx.canvasGetImageData({
      canvasId: 'extractCanvas',
      x: 0, y: 0, width: 100, height: 100,
      success: (data) => {
        // 分析像素，提取主色调
        const colors = this.analyzePixels(data.data)
        this.setData({ generatedColors: colors })
      }
    })
  })
}
```

---

## 三、测试建议

### 功能测试清单
- [ ] 登录 → 输入用户名密码 → 跳转首页
- [ ] 注册 → 填写信息 → 返回登录页
- [ ] 跳过登录 → 直接进入首页
- [ ] 首页情绪选择 → 跳转result页
- [ ] result页色板列表 → 点击进入detail
- [ ] detail页复制颜色 → 复制到剪贴板
- [ ] 工具页配色生成器 → 切换模式生成配色
- [ ] 工具页摇一摇 → 随机生成配色
- [ ] Canvas画板 → 绘制图形
- [ ] Gallery切换tab → 筛选色板
- [ ] Mine页日历显示 → 显示打卡记录
- [ ] Mine页退出登录 → 返回login页

### 边界测试
- 空输入登录
- 无效颜色hex值
- Canvas空操作
- 摇一摇连续触发

---

## 四、优先级排序

### 高优先级（必须完成）
1. 修复home/mine登录检测死循环
2. 实现云开发或简单后端
3. 数据从mock改为数据库读取

### 中优先级（提升体验）
4. 打卡功能真正保存
5. 收藏功能持久化
6. Canvas兼容性修复

### 低优先级（锦上添花）
7. GPS天气推荐
8. 拍照取色真正提取
9. 分享功能

---

## 五、快速完成方案（云开发）

如果时间紧张，推荐使用微信云开发：

```javascript
// app.js
App({
  onLaunch() {
    wx.cloud.init({
      env: '你的云环境ID'
    })
  }
})

// 直接使用云数据库
const db = wx.cloud.database()
db.collection('palettes').get() // 获取色板
db.collection('moods').add({ data: {...} }) // 添加打卡
```

**云开发优势：**
- 无需购买服务器
- 无需域名备案
- 直接在开发者工具中管理
- 符合课设"与服务器通讯"要求

---

## 六、文档与答辩准备

### 需要准备的文档
1. 功能说明文档
2. 数据流程图
3. API接口文档
4. 测试报告

### 答辩演示要点
1. 登录注册流程
2. 情绪→配色推荐
3. 摇一摇演示（加分项）
4. Canvas画板（加分项）
5. 情绪色历（打卡记录）

---

*生成时间：2025-05-26*